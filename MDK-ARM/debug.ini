FUNC void displayvalues(void) {
  int idx,i;
 
  exec("log > MyValues.log");
  for (idx = 0; idx <= 1000; idx++) {
    printf ("%.2f\t%f\n", idx*0.02,fric_2212[1].speed);
//		for (i = 0; i < 37; i++);
  }
  exec("log off");
}
